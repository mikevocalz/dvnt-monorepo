import PricingClient from './pricing-client';

export const metadata = {
  title: 'Membership & Pricing · DVNT',
  description:
    'DVNT Membership unlocks the best of our app and events. Every membership includes Sneaky Lynk access.',
};

export default function Page() {
  return <PricingClient />;
}
