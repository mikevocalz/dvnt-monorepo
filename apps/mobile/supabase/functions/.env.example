# Edge-function secrets. Copy to .env.local for `supabase functions serve --env-file`.
# Production values are set in the dashboard: Edge Functions → Secrets.

# Didit KYB/KYC webhook signing secret (per-destination secret_shared_key, shown
# once at destination creation). Used by supabase/functions/didit-webhook.
DIDIT_WEBHOOK_SECRET=
