// Lynk Live (MoQ livestream) room route — re-exports the feature screen.
// Metro resolves index.native.tsx (mobile viewer) / index.web.tsx (broadcaster+viewer).
export { default } from "@dvnt/app/features/screens/(protected)/lynk/[roomId]";
