import { PublicLockedScreen } from "@dvnt/app/components/access/PublicLockedScreen";

export default function PublicCreateScreen() {
  return <PublicLockedScreen reason="create" kicker="POSTING LOCKED" />;
}
