import { PublicLockedScreen } from "@dvnt/app/components/access/PublicLockedScreen";

export default function PublicActivityScreen() {
  return <PublicLockedScreen reason="activity" kicker="ACTIVITY LOCKED" />;
}
