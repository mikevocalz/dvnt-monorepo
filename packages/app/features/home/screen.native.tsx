/**
 * Home / Feed screen — NATIVE variant (@dvnt/app/features/home/screen).
 * Renders the existing native home (LegendList feed + stories + native tabs
 * machinery). The web sibling (screen.web.tsx) uses TanStack Virtual.
 */
export { default, default as HomeScreen } from "../routes/screens/(protected)/(tabs)/index";
